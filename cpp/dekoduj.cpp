/*
 * dekoduj.cpp
 */


#include <iostream>

using namespace std;

void ascii() {
    int i = 0;
    for (i = 65; i < 91; i++) {
        cout << i << " - " << char(i) << endl;
    }
}
void koduj(char tabzn; rozmiar) {
    
    //for (int i = 0; i < rozmiar; i++) {
        //cout << tabzn[i] << " - " << (int)tabzn[i] << endl;
}

void dekoduj(int tabliczb; rozmiar) {
    ;
}

int main(int argc, char **argv)
{
    int rozmiar = 11;
    char rozmiar[napis] = "Ala ma kota!";
    koduj(tekst, rozmiar);
    int szyfr[11] = {65, 78, 90}
    dekoduj(szyfr,rozmiar)
    
    ascii();
    liczby2litery(rozmiar, napis);
    
	return 0;
}
